# === 任务 2 / 12：特征提取与传统分类器 ===
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
scipy>=1.11

# === 任务 4：GloVe / Word2Vec 词向量 ===
# gensim 4.x 起 KeyedVectors API 有破坏性变更（wv.vocab → wv.key_to_index，
# 不再有 init_sims），任务 5 的预处理代码按 4.x 适配。
gensim>=4.3

# === 任务 6-11：神经网络模型 ===
# CPU 环境直接 pip install torch 即可；需要 CUDA 时按官网给的 index-url 安装，
# 例如：pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.1
tqdm>=4.66

# === 任务 11（选做）：BERT / DistilBERT / RoBERTa ===
transformers>=4.40
datasets>=2.18
accelerate>=0.30

# === 可视化与 Notebook ===
matplotlib>=3.8
jupyterlab>=4.0

# === 测试 ===
pytest>=8.0
